name: Build and Push Docker Images

on:
  push:
    paths:
      - 'hacks/auto-backend*'
      - 'atlantis/**'
      - '.devcontainer/**'
      - '.github/workflows/build-atlantis.yaml'
  workflow_dispatch:
    inputs:
      version_override:
        description: 'Override version tag'
        required: false

jobs:
  prepare:
    if: ${{ github.event.inputs.version_override == '' }}
    name: Prepare variables
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.prepare.outputs.version }}
    steps:
    - name: Prepare variables
      id: prepare
      shell: bash
      run: |
        BRANCH=$(echo ${GITHUB_REF#refs/heads/})
        COMMIT_SHA_SHORT=$(echo ${GITHUB_SHA} | cut -c1-7)
        echo "::set-output name=version::$(echo ${BRANCH}-${COMMIT_SHA_SHORT})"

  build:
    needs: prepare
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1 
      with:
        registry: ghcr.io
        username: ${{ secrets.GH_CR_USERNAME }}
        password: ${{ secrets.GH_CR_TOKEN }}

    - name: Build and push Atlantis Docker image
      uses: docker/build-push-action@v2
      with:
        context: .
        file: ./atlantis/Dockerfile
        push: true
        tags: |
          ghcr.io/${{ secrets.GH_CR_USERNAME }}/${{ github.repository }}/atlantis:${{ github.event.inputs.version_override || needs.prepare.outputs.version }}
          ${{ github.event.inputs.version_override != '' && 'ghcr.io/' + secrets.GH_CR_USERNAME + '/' + github.repository + '/atlantis:latest' }}

    - name: Build and push Devcontainer Docker image
      uses: docker/build-push-action@v2
      with:
        context: ./.devcontainer
        file: ./.devcontainer/Dockerfile
        push: true
        tags: |
          ghcr.io/${{ secrets.GH_CR_USERNAME }}/${{ github.repository }}/devcontainer:${{ github.event.inputs.version_override || needs.prepare.outputs.version }}
          ${{ github.event.inputs.version_override != '' && 'ghcr.io/' + secrets.GH_CR_USERNAME + '/' + github.repository + '/devcontainer:latest' }}
